import { Metadata } from "next";
import IconView from "./components/IconView";

export const metadata: Metadata = { title: "Icon View" };

const IconViewPage = () => {
  return <IconView />;
};

export default IconViewPage;
